Place SSL certificate files here for Nginx.

Expected filenames:
- `fullchain.pem`
- `privkey.pem`

For local Docker testing, you can use a self-signed certificate for `localhost`.
For production, use the real certificate and private key issued for your domain.
